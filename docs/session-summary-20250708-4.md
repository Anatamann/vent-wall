I have updated the `MoodTagFilter` component to improve its mobile usability. The following changes were made:

- Replaced the complex JavaScript-based tag visibility calculation with a simpler and more performant CSS-based horizontal scrolling container.
- Increased the padding of the tags to make them larger and easier to tap on mobile devices.
- Added a gradient fade effect to the right side of the tag list to indicate that it is scrollable.