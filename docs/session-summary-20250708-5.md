I have updated the `VentCard` component to improve its mobile usability and accessibility. The following changes were made:

- Increased the font size of the vent content for better readability on mobile devices.
- Added an `aria-label` to the user avatar to improve accessibility for screen readers.
- Wrapped the timestamp in a `<time>` element with a `dateTime` attribute for better semantic HTML.